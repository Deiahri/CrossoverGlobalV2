import NotFoundView from '@/_components/NotFoundView'

/** Used when a page inside the site group calls notFound() (bad slug, etc.). */
export default function NotFound() {
  return <NotFoundView />
}
